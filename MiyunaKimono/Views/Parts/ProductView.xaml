<UserControl x:Class="MiyunaKimono.Views.Parts.ProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="800">
    <Grid>
        <DataGrid x:Name="GridProducts"
              AutoGenerateColumns="False"
              AlternationCount="1000000"
              HeadersVisibility="Column"
              CanUserAddRows="False" CanUserDeleteRows="False"
              IsReadOnly="True" RowHeaderWidth="0" GridLinesVisibility="None"
              ColumnHeaderHeight="38" RowHeight="56">

            <DataGrid.Columns>

                <!-- ลำดับ -->
                <DataGridTemplateColumn Header="" Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock HorizontalAlignment="Center"
                         Text="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow},
                                        Path=AlternationIndex,
                                        Converter={StaticResource IntIndexTo1Based}}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Thumbnail (รูปแรก) -->
                <DataGridTemplateColumn Header="" Width="70">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Width="46" Height="46" CornerRadius="6" Background="#EEE"
                      HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Image Source="{Binding ThumbPath}" Stretch="UniformToFill"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Product code" Binding="{Binding ProductCode}" Width="160"/>
                <DataGridTextColumn Header="Product name" Binding="{Binding ProductName}" Width="*"/>
                <DataGridTextColumn Header="category" Binding="{Binding Category}" Width="220"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat={}{0:N0} THB}" Width="150"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="120"/>

                <DataGridTemplateColumn Header="" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Edit"
                      Background="#1177FF" Foreground="White"
                      Padding="12,4" BorderBrush="{x:Null}" Width="70"
                      HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
