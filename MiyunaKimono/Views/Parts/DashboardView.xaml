<UserControl x:Class="MiyunaKimono.Views.Parts.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Background="Transparent"
             d:DesignHeight="800" d:DesignWidth="1000">

    <UserControl.Resources>
        <Style x:Key="StatCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderBrush" Value="#EEE"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="MinHeight" Value="120"/>
        </Style>

        <Style x:Key="AdminDataGridRowStyle" TargetType="{x:Type DataGridRow}">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F9FAFB"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#F9FAFB"/>
                    <Setter Property="Foreground" Value="#111827"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="AdminDataGridCellStyle" TargetType="{x:Type DataGridCell}">
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="Padding" Value="12,16"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#111827"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="AdminDataGridHeaderStyle" TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,14"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style x:Key="AdminDataGridStyle" TargetType="{x:Type DataGrid}">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="ColumnHeaderHeight" Value="44"/>
            <Setter Property="RowHeight" Value="56"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <UniformGrid Grid.Row="0" Columns="4" Margin="0,0,0,16">
            <Border Style="{StaticResource StatCard}" Margin="0,0,16,0">
                <StackPanel>
                    <TextBlock Text="TOTAL SALES" FontSize="13" FontWeight="SemiBold" Foreground="#888"/>
                    <TextBlock Text="{Binding TotalSales, StringFormat={}{0:N0} THB}" FontSize="28" FontWeight="Bold" Foreground="#111" Margin="0,12,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCard}" Margin="16,0,16,0">
                <StackPanel>
                    <TextBlock Text="TOTAL ORDERS" FontSize="13" FontWeight="SemiBold" Foreground="#888"/>
                    <TextBlock Text="{Binding TotalOrders, StringFormat={}{0:N0}}" FontSize="28" FontWeight="Bold" Foreground="#111" Margin="0,12,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCard}" Margin="16,0,16,0">
                <StackPanel>
                    <TextBlock Text="NEW CUSTOMER" FontSize="13" FontWeight="SemiBold" Foreground="#888"/>
                    <TextBlock Text="{Binding TotalCustomers, StringFormat={}{0:N0}}" FontSize="28" FontWeight="Bold" Foreground="#111" Margin="0,12,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCard}" Margin="16,0,0,0">
                <StackPanel>
                    <TextBlock Text="REPORT ประจำเดือน" FontSize="13" FontWeight="SemiBold" Foreground="#888"/>
                    <Grid Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <ComboBox Style="{StaticResource ModernComboBox}"
                                  ItemsSource="{Binding MonthOptions}"
                                  SelectedItem="{Binding SelectedMonth}"/>
                        <ComboBox Grid.Column="2" Style="{StaticResource ModernComboBox}"
                                  ItemsSource="{Binding YearOptions}"
                                  SelectedItem="{Binding SelectedYear}"/>
                    </Grid>
                    <Button Content="Report" Style="{StaticResource BlueRoundedButton}" Height="36" Margin="0,10,0,0" IsEnabled="False"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Border Grid.Row="1" Style="{StaticResource StatCard}" Padding="0" Margin="0,0,0,16">
            <DockPanel LastChildFill="True">
                <Grid DockPanel.Dock="Top" Margin="20,16,20,16">
                    <TextBlock Text="TOP SELLING PRODUCT" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <Button Content="View All" Style="{StaticResource TopTextButton}" HorizontalAlignment="Right" IsEnabled="False"/>
                </Grid>

                <DataGrid ItemsSource="{Binding TopSellingItems}"
                          Style="{StaticResource AdminDataGridStyle}"
                          ColumnHeaderStyle="{StaticResource AdminDataGridHeaderStyle}"
                          RowStyle="{StaticResource AdminDataGridRowStyle}"
                          CellStyle="{StaticResource AdminDataGridCellStyle}"
                          ScrollViewer.VerticalScrollBarVisibility="Auto">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="No." Binding="{Binding Index}" Width="50"/>
                        <DataGridTextColumn Header="Product code" Binding="{Binding ProductCode}" Width="*"/>
                        <DataGridTextColumn Header="Product name" Binding="{Binding ProductName}" Width="2*"/>
                        <DataGridTextColumn Header="category" Binding="{Binding Category}" Width="*"/>
                        <DataGridTextColumn Header="Price" Binding="{Binding PriceText}" Width="*"/>
                        <DataGridTextColumn Header="Total Order" Binding="{Binding TotalOrdersText}" Width="*"/>
                        <DataGridTextColumn Header="Total Sale" Binding="{Binding TotalSaleText}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource StatCard}" Padding="0">
            <DockPanel LastChildFill="True">
                <Grid DockPanel.Dock="Top" Margin="20,16,20,16">
                    <TextBlock Text="MOST RECENT TRANSACTIONS" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <Button Content="View All" Style="{StaticResource TopTextButton}" HorizontalAlignment="Right" IsEnabled="False"/>
                </Grid>

                <DataGrid ItemsSource="{Binding RecentTransactions}"
                          Style="{StaticResource AdminDataGridStyle}"
                          ColumnHeaderStyle="{StaticResource AdminDataGridHeaderStyle}"
                          RowStyle="{StaticResource AdminDataGridRowStyle}"
                          CellStyle="{StaticResource AdminDataGridCellStyle}"
                          ScrollViewer.VerticalScrollBarVisibility="Auto">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding DateText}" Width="*"/>
                        <DataGridTextColumn Header="Order ID" Binding="{Binding DisplayId}" Width="*"/>
                        <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="2*"/>
                        <DataGridTextColumn Header="Amount" Binding="{Binding AmountText}" Width="*"/>
                        <DataGridTextColumn Header="Order Status" Binding="{Binding Status}" Width="*"/>
                        <DataGridTemplateColumn Header="Action" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Details"
                                            Style="{StaticResource EditButton}"
                                            HorizontalAlignment="Center"
                                            Click="Details_Click"
                                            Tag="{Binding OrderId}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

    </Grid>
</UserControl>