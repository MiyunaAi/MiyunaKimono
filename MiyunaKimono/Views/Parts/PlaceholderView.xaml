<UserControl x:Class="MiyunaKimono.Views.Parts.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="400">
    <Grid>
        <StackPanel>
            <!-- หัวเรื่อง ใช้สไตล์ HeaderTitle -->
            <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Style="{StaticResource HeaderTitle}"/>

            <!-- เนื้อหา placeholder ชั่วคราว -->
            <Border Background="#F5F6F8" CornerRadius="12" Padding="24" Margin="0,0,0,0">
                <TextBlock Text="Coming soon..." Foreground="#666" FontSize="14"/>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>