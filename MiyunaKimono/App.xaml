<Application x:Class="MiyunaKimono.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MiyunaKimono"
             StartupUri="Views/LoginWindow.xaml">
    <Application.Resources>

        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <!-- วาง/คำนวณเลย์เอาต์ให้ลงพิกเซลจริง -->

        <!-- แถบขาวด้านบน -->
        <Style x:Key="TopWhiteBar" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="20,14"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="BorderBrush" Value="#E9E9EC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- การ์ดสีขาวคลุมคอนเทนต์ด้านล่าง -->
        <Style x:Key="CardWhite" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="18"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="BorderBrush" Value="#E9E9EC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        
        <Style TargetType="{x:Type Window}">
            <Setter Property="UseLayoutRounding" Value="True"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="UseLayoutRounding" Value="True"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <!-- ค่า default ของตัวอักษร (ทั้งแอป) -->
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <!-- ฟอนต์ UI มาตรฐาน -->
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
            <Setter Property="TextOptions.TextRenderingMode"  Value="ClearType"/>
            <Setter Property="TextOptions.TextHintingMode"    Value="Fixed"/>
            <Setter Property="SnapsToDevicePixels"            Value="True"/>
            
        </Style>

        <!-- สไตล์ “หัวเรื่อง” ใช้กับทุกหน้า -->
        <Style x:Key="HeaderTitle" TargetType="{x:Type TextBlock}">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="LineStackingStrategy" Value="MaxHeight"/>
            <!-- หัวเรื่องมักคมกว่าเมื่อใช้ Ideal -->
            <Setter Property="TextOptions.TextFormattingMode" Value="Ideal"/>
            <Setter Property="TextOptions.TextRenderingMode"  Value="Grayscale"/>
            <Setter Property="TextOptions.TextHintingMode"    Value="Fixed"/>
            <Setter Property="SnapsToDevicePixels"            Value="True"/>
        </Style>

        <!-- ปุ่มแบบลิงก์ (ใช้กับ "Forgot password") -->
        <Style x:Key="GhostLinkButton" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#666"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter HorizontalAlignment="Right"
                              VerticalAlignment="Center"
                              RecognizesAccessKey="True"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#666"/>
                    <Setter Property="Background" Value="Transparent"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="#AAA"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- กล่อง Sidebar -->
        <Style x:Key="SideBar" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="20,18"/>
        </Style>

        <!-- หัวข้อ Kimono Admin -->
        <Style x:Key="SideBarTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <!-- ปุ่มเมนูชิดซ้าย (พื้นหลังใส + hover สว่างนิด) -->
        <Style x:Key="SideMenuButton" TargetType="Button" BasedOn="{StaticResource GhostLinkButton}">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,4,0,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="Transparent" CornerRadius="10" Padding="10,8">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#F4F4F6"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#ECECEF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ใช้กับ Log out ที่ด้านล่างให้ชิดซ้ายและไม่ใหญ่เกิน -->
        <Style x:Key="SideLogoutButton" TargetType="Button" BasedOn="{StaticResource SideMenuButton}">
            <Setter Property="Margin" Value="0,8,0,0"/>
        </Style>


        <local:IntIndexTo1BasedConverter x:Key="IntIndexTo1Based"/>
        <!-- 3) ปุ่มสีฟ้าขอบมน ใช้แทน CornerRadius บน Button -->
        <Style x:Key="BlueRoundedButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#1177FF"/>
            <Setter Property="Padding" Value="18,8"/>
            <Setter Property="BorderBrush" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- แปลงลำดับ 0-based -> 1-based ให้ DataGrid -->
        

        <!-- Hyperlink: สีเทา, ไม่ขีดเส้น, ไม่เปลี่ยนสีเมื่อ hover, ไม่รับโฟกัส -->
        <Style TargetType="{x:Type Hyperlink}">
            <Setter Property="Foreground" Value="#666"/>
            <Setter Property="TextDecorations" Value="{x:Null}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="False"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#666"/>
                    <Setter Property="TextDecorations" Value="{x:Null}"/>
                </Trigger>
            </Style.Triggers>
        </Style>



        <!-- CheckBox แบน: ตัดกรอบโฟกัส/ไม่เปลี่ยนสีน้ำเงิน -->
        <Style x:Key="FlatCheckBox" TargetType="{x:Type CheckBox}">
            <Setter Property="Foreground" Value="#666"/>
            <Setter Property="TextElement.Foreground" Value="#666"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border x:Name="Box" Width="16" Height="16"
                      CornerRadius="3" BorderBrush="#999" BorderThickness="1"
                      Background="Transparent" VerticalAlignment="Center"/>

                            <Path x:Name="Tick" Grid.Column="0"
      Width="12" Height="12"
      Stretch="Uniform"
      Data="M 2 8 L 6 12 L 14 4"
      Stroke="#f7a9a9" StrokeThickness="2.2"
      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
      HorizontalAlignment="Center" VerticalAlignment="Center"
      SnapsToDevicePixels="True"
      Visibility="Collapsed"/>

                            <ContentPresenter Grid.Column="1" Margin="8,0,0,0"
                                VerticalAlignment="Center"
                                RecognizesAccessKey="True"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Tick" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Box" Property="BorderBrush" Value="#999"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ปุ่มไอคอน (ใช้กับปุ่มตา/ปุ่มไอคอนอื่น ๆ ไม่มีโฮเวอร์ฟ้า) -->
        <Style x:Key="IconButtonNoHover" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <!-- ตัดกรอบโฟกัสคอนโทรลทั่วไป -->
        <Style TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
        <Style TargetType="{x:Type PasswordBox}">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>

        <!-- ปุ่มหลักพื้นชมพู -->
        <Style x:Key="PinkPrimaryButton" TargetType="{x:Type Button}">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#f7a9a9"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ปุ่มขอบชมพู (พื้นโปร่ง) -->
        <Style x:Key="PinkOutlineButton" TargetType="{x:Type Button}">
            <Setter Property="Foreground" Value="#f7a9a9"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="BorderBrush" Value="#f7a9a9"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Watermark สำหรับ TextBox: ใช้ Tag เป็นข้อความ placeholder -->
        <Style x:Key="WatermarkTextBox" TargetType="{x:Type TextBox}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost"/>
                            <TextBlock Text="{TemplateBinding Tag}"
                         Foreground="#999"
                         Margin="2,0,0,0"
                         VerticalAlignment="Center"
                         IsHitTestVisible="False"
                         Visibility="Collapsed">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <!-- แสดง watermark เมื่อ Text ว่าง และไม่ได้โฟกัส -->
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value=""/>
                                                    <Condition Binding="{Binding IsKeyboardFocusWithin, RelativeSource={RelativeSource TemplatedParent}}" Value="False"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Label เหนือช่องกรอก -->
        <Style x:Key="FormLabel" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="#444"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,14,0,6"/>
        </Style>





    </Application.Resources>
</Application>
